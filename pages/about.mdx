import React from "react";
import Card from "../components/card"
import getStarted from "../data/_getStarted.json"
import explore from "../data/_explore.json"

export default function HelloWorld() {
  return (
    <div className="container">
      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-2 justify-between items-center gap-6 py-20">
        <div className="col-span-1">
          <div className="text-xl md:text-2xl lg:text-4xl font-extrabold">
            Welcome to the Fuse docs
          </div>
          <p>
            Fuse is an EVM-compatible blockchain providing a scalable, fast, and
            cost-effective infrastructure for Web3 applications. Catering to
            businesses and consumers, it ensures high transaction volumes,
            security, and decentralization. Dive into our documentation to learn
            more.
          </p>
        </div>
        <img
          src="https://docs.fuse.io/img/home-picture.png"
          className="w-[509px] h-[267px] object-fill col-span-1"
          alt=""
        />
      </div>

      <p className="text-lg md:text-xl lg:text-2xl font-bold">Get Started</p>
      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-2 gap-4 mt-8">
        {getStarted.map((res)=>(
          <Card key={res?.id} res={res}/>
        ))}
      </div>

      <br />
      <br />

      <p className="text-lg md:text-xl lg:text-2xl font-bold">Explore our APIs</p>
      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4 mt-8">
        {explore.map((res)=>(
          <Card key={res?.id} res={res}/>
        ))}
      </div>

    </div>

); }
